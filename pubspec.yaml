name: bentos_driver_sdk
description: BentOS Driver SDK — build CUSE/FUSE drivers in pure Dart.
version: 0.0.1
repository: https://github.com/cafe01/bentos

environment:
  sdk: ^3.8.0
resolution: workspace

publish_to: none

dependencies:
  fixnum: ^1.1.0
  logging: ^1.3.0
  protobuf: ^6.0.0
  stream_channel: ^2.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
